// src/app/auth/login/layout.jsx

export const metadata = {
  title: "StudyNook - Available Rooms",
  description: "Secure login portal for your asset spaces pipeline.",
};

export default function RoomLayout({ children }) {
  return <>{children}</>;
}
